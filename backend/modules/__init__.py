"""Feature modules for the Fooplace backend.

Each subdirectory that contains ``apps.py`` is a Django app, mounted at
``/api/<name>/``. Create one with::

    bazel run //backend:manage -- startmodule <name>
"""
