# Clerk (https://dashboard.clerk.com → API keys)
#
# These are server-only. Do not prefix with VITE_ and do not put them in
# the Vite frontend. Set them on the Django host (not needed on Vercel
# for the static SPA).

# Required to verify Clerk session tokens
CLERK_SECRET_KEY=

# Optional PEM public key for networkless JWT verification
# CLERK_JWT_KEY=

# Comma-separated frontend origins allowed in the token azp claim
CLERK_AUTHORIZED_PARTIES=http://localhost:5173,http://127.0.0.1:5173
